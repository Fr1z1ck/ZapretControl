# Zapret Control Manager

Современный и компактный графический интерфейс для управления инструментом обхода DPI **Zapret**. Приложение обеспечивает удобное переключение между стратегиями, мониторинг доступности сервисов и автоматизацию запуска.

## ✨ Особенности

- **🚀 Быстрый запуск**: Включение и выключение обхода одной кнопкой.
- **📊 Мониторинг сервисов**: Визуальное отображение статуса YouTube и Discord для каждой стратегии.
- **🛠 Удобство**: 
  - Компактный и современный Dark UI.
  - Автоматическое сохранение последней выбранной стратегии.
  - Поддержка автозагрузки вместе с Windows.
  - Сворачивание в системный трей.
- **🔍 Диагностика**: Встроенная проверка доступности сервисов (Health Check) для всех стратегий.

## 📦 Установка

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/ВАШ_ЛОГИН/ZapretControl.git
   cd ZapretControl
   ```

2. **Установите зависимости**:
   Убедитесь, что у вас установлен Python 3.8+.
   ```bash
   pip install -r requirements.txt
   ```

3. **Подготовьте Zapret runtime**:
   - Скачайте последний релиз стратегий: https://github.com/Flowseal/zapret-discord-youtube/releases/latest
   - Распакуйте архив так, чтобы `winws.exe` и `.bin` файлы были в `Zapret/bin`
   - Убедитесь, что `.bat` стратегии находятся в `Zapret/` (например, `general (ALT).bat`)
   - В приложении в «Настройки → Проверить стратегии» можно загрузить актуальные `.bat` из релиза

## 🚀 Запуск

Запустите основной скрипт:
```bash
python main.py
```
Или используйте скомпилированную версию (если доступна).

## 🔄 Загрузка стратегий

- Проверка релизов выполняется по последней версии на GitHub (`Flowseal/zapret-discord-youtube`)
- Загрузка `.bat` выполняется по релизному тегу с проверкой целостности
- Ошибки сети и частичные загрузки обрабатываются и отражаются в интерфейсе

## 🛠 Сборка EXE

Для сборки в один исполняемый файл используйте PyInstaller:
```bash
./ZapretControl.bat
```
Результат будет в папке `dist/ZapretControl.exe`.

## 🤝 Благодарности

- Автору оригинального [Zapret](https://github.com/bol-van/zapret) за мощный инструмент обхода DPI.
- Разработчикам библиотеки PySide6 за отличный фреймворк для UI.

---
**Создатель:** [Fr1z1ck](https://github.com/Fr1z1ck)

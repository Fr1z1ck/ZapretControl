# Zapret DPI Bypass - GUI Control Panel

Современный графический интерфейс для управления Zapret DPI Bypass инструментом.

## 🎨 Особенности

- ✨ Современный и красивый интерфейс на базе CustomTkinter
- 📊 Панель статуса в реальном времени
- 🔍 Встроенная диагностика системы
- ⚙️ Управление службами Windows
- 🎮 Настройка Game Filter и IPSet режимов
- 📥 Проверка и установка обновлений
- 🔄 Управление конфигурациями

## 📋 Требования

- Windows 10/11
- Python 3.8 или выше
- Права администратора (для управления службами)

## 🚀 Установка

### Автоматическая установка

1. Дважды кликните на `install_dependencies.bat` для установки всех зависимостей

### Ручная установка

1. Убедитесь, что у вас установлен Python 3.8 или выше:
   ```bash
   python --version
   ```
   
   Если Python не установлен, скачайте с [python.org](https://www.python.org/downloads/)
   и **обязательно отметьте "Add Python to PATH"** при установке!

2. Установите зависимости одним из способов:
   ```bash
   # Способ 1 (рекомендуется):
   python -m pip install -r requirements.txt
   
   # Способ 2:
   pip install -r requirements.txt
   ```
   
   Если возникает ошибка "pip is not recognized", используйте `python -m pip` вместо просто `pip`

## 💻 Использование

### Запуск приложения

**Способ 1 (рекомендуется):**
- Дважды кликните на `run_gui.bat`

**Способ 2:**
```bash
python zapret_gui.py
```

**Важно:** 
- Для полной функциональности (установка/удаление служб) запускайте приложение от имени администратора
- Если `run_gui.bat` не работает, сначала запустите `install_dependencies.bat` для установки зависимостей

### Основные функции

#### ⚙️ Управление службой

- **Установить службу** - Устанавливает zapret как службу Windows с выбранной конфигурацией
  - Откроется окно выбора конфигурации
  - После выбора будет запущен service.bat для установки
  - В открывшемся окне выберите пункт "1. Install Service" и введите номер выбранной конфигурации
- **Удалить службу** - Удаляет службу zapret
- **Запустить службу** - Запускает службу zapret
- **Остановить службу** - Останавливает службу zapret
- **Обновить статус** - Обновляет информацию о текущем состоянии службы

#### 🔍 Поиск рабочего Bypass

- **Найти рабочий Bypass** - Автоматически проверяет все конфигурации на работоспособность
  - Перебирает каждую конфигурацию по очереди
  - Запускает конфигурацию и проверяет доступность YouTube и Discord
  - Показывает иконки для каждой конфигурации:
    - 📺 - работает только YouTube
    - 💬 - работает только Discord
    - 📺💬 - работают оба сервиса
    - ❌ - ничего не работает
  - Если конфигурация работает - останавливает поиск и выделяет найденную
  - Показывает прогресс проверки в реальном времени
  - Время проверки: ~10 секунд на каждую конфигурацию

#### 📊 Статус

Отображает:
- Статус службы zapret (запущена/остановлена)
- Статус процесса winws.exe (активен/неактивен)
- **Статус доступности YouTube** - автоматическая проверка доступности youtube.com
  - ✅ Зеленый индикатор - youtube.com доступен
  - ❌ Красный индикатор - youtube.com недоступен
  - ⚠️ Оранжевый индикатор - ошибка проверки
  - Отображается время последней проверки
  - Кнопка для ручной проверки
- Информацию о текущей конфигурации

#### 🔍 Диагностика

Запускает автоматическую диагностику системы:
- Проверка Base Filtering Engine
- Проверка настроек прокси
- Проверка TCP timestamps
- Проверка конфликтующих сервисов (Adguard, Killer, Intel Connectivity, Check Point, SmartByte, VPN)
- Проверка DNS настроек
- Проверка конфликтов с другими bypass инструментами
- Очистка кэша Discord

#### ⚙️ Настройки

**Game Filter:**
- Включение/выключение фильтра для игр

**IPSet режим:**
- `any` - Разрешить все IP адреса
- `none` - Блокировать все IP адреса (кроме исключений)
- `loaded` - Использовать загруженный список IP адресов

**Проверка работоспособности:**
- При запуске приложения автоматически выполняется проверка доступности YouTube и Discord
- Показывает, работает ли bypass для каждого сервиса
- Кнопка для ручной проверки работоспособности в любое время
- Отображает результат проверки на вкладке "Статус"
- Показывает иконки статуса для каждой конфигурации после проверки

**Обновления:**
- Проверка наличия новых версий

## 🎯 Выбор конфигурации

В разделе "Управление" доступен список всех конфигурационных файлов (.bat), кроме service.bat. 

Вы можете:
- Выбрать конфигурацию из списка
- Запустить выбранную конфигурацию напрямую
- Установить службу с выбранной конфигурацией

## 📝 Примечания

- Некоторые операции требуют прав администратора
- После изменения настроек (Game Filter, IPSet) необходимо перезапустить службу
- При установке службы выберите нужную конфигурацию из списка
- Рекомендуется запускать диагностику перед установкой службы для проверки конфликтов

## 🔧 Устранение неполадок

### Ошибка "pip is not recognized"

Используйте `python -m pip` вместо просто `pip`:
```bash
python -m pip install -r requirements.txt
```

Или запустите `install_dependencies.bat`, который автоматически использует правильную команду.

### Python не найден

1. Убедитесь, что Python установлен: скачайте с [python.org](https://www.python.org/downloads/)
2. При установке обязательно отметьте **"Add Python to PATH"**
3. После установки перезапустите командную строку/терминал
4. Проверьте: `python --version`

### Приложение не может управлять службой

Убедитесь, что:
1. Приложение запущено от имени администратора
2. Служба zapret не установлена в другом месте
3. Нет конфликтующих процессов

### Диагностика показывает ошибки

- Следуйте инструкциям в результатах диагностики
- Удалите конфликтующие сервисы или программы
- Проверьте настройки сети и DNS

### Ошибки при установке зависимостей

Попробуйте обновить pip сначала:
```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

## 📄 Лицензия

Этот GUI является обёрткой для проекта zapret-discord-youtube. Используйте в соответствии с лицензией оригинального проекта.

## 🙏 Благодарности

- Разработчикам zapret-discord-youtube
- Разработчикам CustomTkinter за прекрасную библиотеку UI
